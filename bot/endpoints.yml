# This file contains the different endpoints your bot can use.

action_endpoint:
  url: "http://actions:5055/webhook"     # Deploy (Docker Compose)
  # url: "http://localhost:5055/webhook" # Deploy (Local)
